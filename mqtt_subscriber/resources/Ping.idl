/* Package with an "IPing" interface declaration. */

package mqtt_subscriber.Ping

interface {
    /* Ping method interface. */
    Ping(in UInt32 value, in UInt32 result);
    Pong(in UInt32 task, in UInt32 id,in UInt32 x, in UInt32 y);
}
